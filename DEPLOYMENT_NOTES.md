# Развертывание Elite функционала

## Установленные зависимости

```bash
pip install matplotlib seaborn pandas numpy scikit-learn joblib fastapi uvicorn
```

## Новые функции (Elite тариф - 999₽/мес)

### 1. CSV экспорт смен
- Команда: `/export` или кнопка в меню
- Экспорт за последние 30 дней
- Rate limiting: 1 раз/день для обычных пользователей, безлимит для Elite

### 2. Калькулятор налогов НПД
- Команда: `/tax`
- Расчет для самозанятых (4% физлица, 6% юрлица)
- Учет налогового вычета до 10,000₽

### 3. Тепловая карта заработка
- Команда: `/heatmap`
- Визуализация по часам × дням недели
- Анализ последних 90 дней

### 4. Расширенная статистика
- Периоды: 7/14/30 дней
- Детальная аналитика смен, заработка, километража

### 5. REST API
- Запуск: `python -m uvicorn bot.api.app:app --host 0.0.0.0 --port 8000`
- Документация: http://localhost:8000/api/docs
- Endpoints:
  - GET /api/v1/shifts/export - экспорт смен
  - GET /api/v1/stats/summary - статистика
  - GET /api/v1/predictions/demand - предсказания спроса

### 6. ML предсказания
- Модель: RandomForest
- Требует: 60 дней исторических данных
- Автоматический сбор данных каждые 8 минут

## Миграции БД

```bash
# Применить миграции
python apply_ml_migration.py
```

## Туннель для WebApp (карта)

### Проблема ngrok
Бесплатный ngrok показывает предупреждение "Visit Site" перед каждым посещением.

### Решение: Cloudflare Tunnel (РЕКОМЕНДУЕТСЯ)

1. Скачать cloudflared: https://github.com/cloudflare/cloudflared/releases/latest
2. Положить `cloudflared.exe` в папку проекта
3. Запустить: `start_cloudflare_tunnel.bat`
4. Скопировать URL из консоли
5. Обновить WebApp URL в боте: `/setwebapp https://your-url.trycloudflare.com`

**Преимущества:**
- ✅ Бесплатно
- ✅ Без предупреждений
- ✅ Стабильнее ngrok
- ✅ Подходит для 100+ пользователей

Подробнее: см. `TUNNEL_SETUP.md`

## Статус

✅ Бот запущен и работает
✅ Все handlers зарегистрированы
✅ Scheduler активен
✅ Сбор исторических данных включен
⚠️ Требуется настройка туннеля (см. выше)

## Коммиты

- 054bbbe - Добавлен Elite тариф и расширенный функционал
- 0863d4d - Fix: исправлен импорт session_factory

## Статистика

- 22 новых файла
- 1,727 строк кода
- 4 handlers, 4 services, 8 API файлов, 1 ML модуль
